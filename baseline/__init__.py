# baseline/ — reference agents used to establish lower and upper performance bounds.
#
# random_agent.py    : picks actions uniformly at random — the weakest possible baseline.
# rule_based_agent.py: uses handcrafted clinical rules — a strong non-ML upper bound.
#
# Run either script directly (python baseline/random_agent.py) to benchmark against
# these baselines before comparing your own agent's scores.
